#include<iostream>
using namespace std;

int main() {

    int num;

    cin>>num;

    if(num==0) cout<<"YONSEI";
    else if (num==1) cout<<"Leading the Way to the Future";

    return 0;
}